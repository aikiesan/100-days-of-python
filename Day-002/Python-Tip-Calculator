print("Welcome to the tip calculator!")
bill = float(input("What was the total bill? $USD\n"))
tip = int(input("What percentage tip would you like to give? 10%, 12% or 15% \n"))
people = int(input("How many people would like to split the bill?\n"))
total = (bill + (bill * tip / 100)) / people  # Calculate bill with tip, then divide
print(f"The total amount for the split is: ${total} USD")
